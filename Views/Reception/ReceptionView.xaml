<Window x:Class="test2.Views.Reception.ReceptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản Lý Tiếp Nhận Xe" Height="750" Width="1250"
        WindowStartupLocation="CenterScreen" FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#4E73DF"/>
        <Style x:Key="CenterTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="LeftTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F8F9FC">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#1A1D21">
            <TextBlock Text="GARA AUTO" Foreground="White" HorizontalAlignment="Center" Margin="0,30" FontSize="20" FontWeight="Bold"/>
        </Grid>

        <Grid Grid.Column="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Danh sách xe đang tiếp nhận" FontSize="26" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Border Background="White" CornerRadius="8" BorderBrush="#E3E6F0" BorderThickness="1" HorizontalAlignment="Left" Width="350" Height="40">
                    <Grid>
                        <TextBlock Text="🔍" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#B7B9CC" HorizontalAlignment="Left"/>
                        <TextBox x:Name="txtSearch" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="35,0,10,0" TextChanged="TxtSearch_TextChanged"/>
                    </Grid>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="📥 Xuất Excel" Background="#1CC88A" Foreground="White" Padding="15,0" Height="40" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button x:Name="btnBackToService" Content="⬅ Quản lý dịch vụ" Click="btnBackToService_Click" Background="#858796" Foreground="White" Padding="15,0" Height="40" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button x:Name="btnAddReception" Content="+ Tiếp nhận xe mới" Click="btnAddReception_Click" Background="{StaticResource PrimaryBlue}" Foreground="White" Padding="20,0" Height="40" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="#E3E6F0" BorderThickness="1" Padding="10">
                <DataGrid x:Name="dgReceptions" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="50">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="60" ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Biển số" Binding="{Binding LicensePlate}" Width="120" ElementStyle="{StaticResource CenterTextStyle}" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Tên Chủ Xe" Binding="{Binding CustomerName}" Width="1.5*" ElementStyle="{StaticResource LeftTextStyle}"/>
                        <DataGridTextColumn Header="Ngày vào" Binding="{Binding EntryDate}" Width="150" ElementStyle="{StaticResource CenterTextStyle}"/>
                        <DataGridTextColumn Header="Tình trạng lúc vào" Binding="{Binding VehicleCondition}" Width="2*" ElementStyle="{StaticResource LeftTextStyle}"/>
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="120" ElementStyle="{StaticResource CenterTextStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Window>